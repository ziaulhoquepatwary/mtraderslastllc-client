export const metadata = {
    title: "Terms & Condition",
    description: "Web Development & Digital Solutions Platform",
};

export default function TermsCondition({ children }) {
    return (
        <main className="mt-15">
            {children}
        </main>
    );
}