export const metadata = {
    title: "Refund Policy",
    description: "Web Development & Digital Solutions Platform",
};

export default function RefundPolicy({ children }) {
    return (
        <main className="mt-15">
            {children}
        </main>
    );
}