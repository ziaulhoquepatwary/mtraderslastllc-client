export const metadata = {
    title: "AMLPolicy",
    description: "Web Development & Digital Solutions Platform",
};

export default function AMLPolicy({ children }) {
    return (
        <main className="mt-15">
            {children}
        </main>
    );
}